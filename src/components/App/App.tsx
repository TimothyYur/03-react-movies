import { useState } from 'react';
import type { VoteType, Votes } from '../../types/movie';
import CafeInfo from '../SearchBar/SearchBar';
import Notification from '../MoovieGrid/MoovieGrid';
import VoteOptions from '../Loader/Loader';
import VoteStats from '../ErrorMessage/ErrorMessage';
import css from './App.module.css';

export default function App() {

  return (
    <div className={css.app}>
      
    </div>
  );
}
